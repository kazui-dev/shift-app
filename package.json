{
  "name": "shift-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "deploy": "pnpm --filter api run deploy"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "turbo": "^2.10.10",
    "typescript": "~6.0.3"
  },
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
